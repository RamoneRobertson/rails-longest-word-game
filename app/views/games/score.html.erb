<div class="container">
  <div class="row">
    <div class="col">
      <h1>Result</h1>
      <span>Score: <strong><%= @total_score %></strong></span>
      <% if @in_grid%>
        <% if @valid %>
          <p>Congratulations! <strong><%= @word.upcase! %></strong> is a valid english word!</p>
        <% else %>
          <p>:( Try again... <strong><%= @word.upcase! %></strong> doesnt seem to be a valid English word</p>
        <% end %>
      <% else %>
        <p>Sorry but <strong><%= @word.upcase %></strong> cant be built out of <strong><%= @letters.join(", ").upcase %></strong></p>
      <% end %>
    <%= link_to "Play Again", new_path %>
    </div>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-12">
      <div class="game-container m-5">
        <h1>New Game</h1>
        <div class="letters-grid mb-5">
          <% @letters.each do |letter| %>
            <div class="letter-block"><%= letter %></div>
          <% end %>
        </div>
        <h2>What is the longest word you can find?</h2>
        <form action="/score" method="POST" data-turbo="false">
          <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
          <%= hidden_field_tag :letters, @letters %>
          <div class="input-group mb-3">
            <input type="text" name="word" class="form-control" placeholder="Type answer here">
            <button class="btn btn-outline-secondary" type="submit" id="button-addon2">Play</button>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
